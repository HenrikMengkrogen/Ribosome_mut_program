[package]
name = "Ribosome_mut_program"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10"
librna-sys = { version = "0.3", features = ["auto"] }
libc = "0.2"
rayon = "1.10"

[build-dependencies]
pkg-config = "0.3"

